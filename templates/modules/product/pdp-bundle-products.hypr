{%if model.bundledProducts.length > 0 %}
    <div> 
        The {{model.content.productName}} includes
    </div>
    {% for components in model.bundledProducts %}	
        <div class="bundle-count">
            Qty. {{components.quantity}} - {{components.content.productName}}
        </div>
    {% endfor %}
{%endif %}
